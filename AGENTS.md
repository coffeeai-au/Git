# AGENTS.md

## Cursor Cloud specific instructions

This repository (`hi-tech-espresso/Git`) currently contains only an Apache 2.0 LICENSE file and no application code, build configuration, or dependencies.

- **No services to run.** There are no backend, frontend, or auxiliary services.
- **No lint / test / build commands.** No tooling is configured.
- **No dependency files.** No `package.json`, `requirements.txt`, `go.mod`, `Cargo.toml`, or similar.

When application code is added, update this section with:
1. How to install dependencies (package manager, lock file).
2. How to run lint, tests, and the dev server.
3. Any non-obvious environment setup caveats.
